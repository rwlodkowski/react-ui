import styled from "styled-components";
import StyledIcon from "./StyledIcon";
import chromecast from "./svg/chromecast.svg";

export const ChromecastIcon = styled(StyledIcon)`
  background-image: url(${chromecast});
`;
