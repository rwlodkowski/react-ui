import styled from "styled-components";
import StyledIcon from "./StyledIcon";
import language from "./svg/language.svg";

export const LanguageIcon = styled(StyledIcon)`
  background-image: url(${language});
`;
