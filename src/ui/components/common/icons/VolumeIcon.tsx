import styled from "styled-components";
import StyledIcon from "./StyledIcon";
import volume from "./svg/volume.svg";

export const VolumeIcon = styled(StyledIcon)`
  background-image: url(${volume});
`;
