import styled from "styled-components";
import StyledIcon from "./StyledIcon";
import pause from "./svg/pause.svg";

export const PauseIcon = styled(StyledIcon)`
  background-image: url(${pause});
`;
