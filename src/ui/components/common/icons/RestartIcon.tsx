import styled from "styled-components";
import StyledIcon from "./StyledIcon";
import restart from "./svg/restart.svg";

export const RestartIcon = styled(StyledIcon)`
  background-image: url(${restart});
`;
