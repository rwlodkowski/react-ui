import BaseButton from "./BaseButton";
import { Rewind30Icon } from "../common/icons";
import styled from "styled-components";

const Rewind30Button = styled(BaseButton).attrs({ icon: Rewind30Icon })``;

export default Rewind30Button;
