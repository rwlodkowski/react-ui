import BaseButton from "./BaseButton";
import { Forward10Icon } from "../common/icons";
import styled from "styled-components";

const Forward10Button = styled(BaseButton).attrs({ icon: Forward10Icon })``;

export default Forward10Button;
