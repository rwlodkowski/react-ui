import BaseButton from "./BaseButton";
import { Rewind10Icon } from "../common/icons";
import styled from "styled-components";

const Rewind10Button = styled(BaseButton).attrs({ icon: Rewind10Icon })``;

export default Rewind10Button;
