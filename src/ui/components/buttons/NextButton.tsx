import BaseButton from "./BaseButton";
import { NextIcon } from "../common/icons";
import styled from "styled-components";

const NextButton = styled(BaseButton).attrs({
  icon: NextIcon
})``;

export default NextButton;
