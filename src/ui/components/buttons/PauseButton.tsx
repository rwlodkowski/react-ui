import BaseButton from "./BaseButton";
import { PauseIcon } from "../common/icons";
import styled from "styled-components";

const PauseButton = styled(BaseButton).attrs({ icon: PauseIcon })``;

export default PauseButton;
