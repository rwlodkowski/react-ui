import BaseButton from "./BaseButton";
import { Forward30Icon } from "../common/icons";
import styled from "styled-components";

const Forward30Button = styled(BaseButton).attrs({ icon: Forward30Icon })``;

export default Forward30Button;
