import BaseButton from "./BaseButton";
import { RestartIcon } from "../common/icons";
import styled from "styled-components";

const RestartButton = styled(BaseButton).attrs({ icon: RestartIcon })``;

export default RestartButton;
