import BaseButton from "./BaseButton";
import { PlayIcon } from "../common/icons";
import styled from "styled-components";

const PlayButton = styled(BaseButton).attrs({ icon: PlayIcon })``;

export default PlayButton;
