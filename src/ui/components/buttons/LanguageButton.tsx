import BaseButton from "./BaseButton";
import { LanguageIcon } from "../common/icons";
import styled from "styled-components";

const LanguageButton = styled(BaseButton).attrs({ icon: LanguageIcon })``;

export default LanguageButton;
