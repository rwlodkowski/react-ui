import BaseButton from "./BaseButton";
import { ChannelsListIcon } from "../common/icons";
import styled from "styled-components";

const ChannelsListButton = styled(BaseButton).attrs({
  icon: ChannelsListIcon
})``;

export default ChannelsListButton;
