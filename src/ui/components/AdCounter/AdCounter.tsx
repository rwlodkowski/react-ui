import styled from "styled-components";
import Text from "../common/Text";

interface Props {}

const AdCounter = styled(Text)`
  display: inline-block;
  font-size: 13px;
`;

export default AdCounter;
